<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    
    <style name="AlertDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>

    <!-- estilo para o SearchView -->
    <style name="MaterialSearchViewStyle">

        <item name="searchBackIcon">@drawable/ic_backspace_white_24dp</item>
        <item name="searchCloseIcon">@drawable/ic_close_white_24dp</item>
        <item name="searchBackground">@color/colorPrimary</item>
        <item name="android:textColor">@color/textLithing</item>
        <item name="android:textColorHint">@color/textLithing</item>
        <item name="android:hint">@string/hintSearView</item>

    </style>

</resources>
